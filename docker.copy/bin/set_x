#!/usr/bin/env bash

if [ -z $INIT ]; then
    SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
    source "${SCRIPT_DIR}/_setup"
fi

echo -e "${GREEN} >>> make scripts executable on \"${SCRIPT_DIR}\""

find "${SCRIPT_DIR}" -maxdepth 1 -type f -exec chmod +x {} +
